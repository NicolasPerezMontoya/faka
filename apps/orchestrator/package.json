{
  "name": "@faka/orchestrator",
  "version": "0.0.0",
  "private": true,
  "description": "faka orchestrator — Hono server + cron on Railway",
  "type": "module",
  "main": "./dist/server.js",
  "scripts": {
    "build": "tsc --noEmit",
    "start": "tsx src/server.ts",
    "start:cron": "tsx src/cron.ts",
    "dev": "tsx watch src/server.ts",
    "lint": "echo '(uses base eslint via app/package configs)'",
    "test": "vitest run --passWithNoTests",
    "test:integration": "vitest run -c vitest.integration.config.ts --passWithNoTests"
  },
  "dependencies": {
    "@ai-sdk/openai": "^2.0.0",
    "@faka/connectors": "workspace:*",
    "@faka/db": "workspace:*",
    "@faka/llm": "workspace:*",
    "@faka/schema": "workspace:*",
    "@hono/node-server": "^1.13.7",
    "@hono/zod-validator": "^0.4.2",
    "@supabase/supabase-js": "^2.105.1",
    "ai": "^6.0.0",
    "hono": "^4.6.14",
    "p-retry": "^7.0.0",
    "pino": "^9.5.0",
    "tsx": "^4.19.2",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@faka/config": "workspace:*",
    "@types/node": "^22.10.5",
    "typescript": "5.7.3",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=22.13"
  }
}
