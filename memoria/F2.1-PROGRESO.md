# Fase 2.1 — Mercado Libre Colombia (OAuth + cascade reuse) — F2.1 CODE-COMPLETE ✓

**Cierre: 2026-05-15. Último commit del ciclo: plan 2.1.5.2 (`phase-2.1 plan 2.1.5.2`).
F2.1 está code-complete. Awaiting cliente OAuth bootstrap + production cutover
(orchestrator deploy a Railway) para flip a CONFIGURED mode.**

## Estado final: 22/22 plans completados, F2.1 complete

F2.1 cierra a las 22 planes con un commit dedicado por plan + un commit
chore intermedio (`c7f64ba chore: gitignore .vercel/ + scripts/demo-bundle/`).
22/22 plans implementados; 6 waves ejecutadas en orden serial-con-paralelismo
dentro de cada wave (ver mermaid en PLAN.md §Execution waves).

**Diferencia con F2:** F2 cerró 25/25 con 1 plan consolidado (2.5.3 fundido
en 2.5.2+2.5.4). F2.1 cierra 22/22 sin consolidaciones — cada plan tiene su
commit dedicado.

## Plan completo (referencia)

Archivos en `.planning/phases/02.1-mercado-libre-colombia-integration-oauth-5-channel-matching-/`:

- `CONTEXT.md`: vision, in-scope, out-of-scope, anti-goals, locked decisions
  (siteId MCO hardcoded, currency COP hardcoded, cascade reuse from F2,
  oauth_tokens unique on (canal, user_id), idempotency on
  (canal, external_order_id)).
- `RESEARCH.md`: ML OAuth lifecycle (6h access + single-use refresh +
  advisory-lock race protection + 5h safety net), webhook signed-query-params
  HMAC (NOT body — diverges from WP/POS pattern), REST API client
  (paginateOrders via from_id + paginateItems via scroll_id scan), catalog-mode
  DLQ (Assumption A2: low confidence MCO 2026 adoption), variant attribute
  hashing (additive unique constraint), per-variation pricing stash under
  atributos_json.__pricing, standard stack table (undici 8.2.0 pinned), 12+
  pitfalls (top: redirect URI exact-match, currency MCO assertion,
  signed-query-params canonical-string order).
- `PATTERNS.md`: mapping de los 18 archivos nuevos a sus analogs en F1/F2
  (oauth_tokens ≈ csv_mapping_profiles RAW-layer table; webhook-verify.ts
  divergente; cascade REUSED-from-F2 invariant; CC-11 + CC-12 + CC-14 carried
  forward).
- `PLAN.md` (22 plans, 74h, 6 waves W0-W5).
- `PLAN-CHECK.md`: VERDICT **PASS** con warnings no-bloqueantes (mismos shape
  que F2's PLAN-CHECK).

> **Anti-duplication:** este memo NO duplica CONTEXT/PATTERNS/RESEARCH.
> Linkea a ellos. Es un *running journal* de progreso.

## Commits por wave

### Wave 0 — Prep + scaffolding ✓

| Plan    | Commit    | Qué hizo                                                                                          |
| ------- | --------- | ------------------------------------------------------------------------------------------------- |
| 2.1.0.1 | `1d43549` | Pin F2.1 deps: undici 8.2.0 (ML REST + OAuth transport) + msw para tests; no axios, no node-fetch |
| 2.1.0.2 | `f0d8463` | Env vars + secrets schema: ML_CLIENT_ID/SECRET/REDIRECT_URI/WEBHOOK_SECRET en .env.example + railway.toml |
| 2.1.0.3 | `f5335c4` | Extend CC-11 eslint regex a `MERCADOLIBRE\|ML_CLIENT\|ML_REDIRECT\|ML_WEBHOOK` (NEXT_PUBLIC_ guard) |
| 2.1.0.4 | `69bd550` | ML test fixtures (orders paid + cancelled + items con variations + token response; PII redacted) |

### Wave 1 — OAuth lifecycle ✓

| Plan    | Commit    | Qué hizo                                                                                          |
| ------- | --------- | ------------------------------------------------------------------------------------------------- |
| 2.1.1.1 | `d462e32` | Migration `20260615000001_oauth_tokens.sql` (service-role-only RLS + unique (canal, user_id)) + types extension |
| 2.1.1.2 | `11e2a1e` | `oauth.ts` — exchange (auth_code → tokens) + refresh (single-use rotation) + advisory_xact_lock race protection + lazy refresh on 401 |
| 2.1.1.3 | `af7c1d5` | ML status → internal sales.estado mapper (paid → pagado, cancelled → cancelado, etc.) |
| 2.1.1.4 | `ab71ddb` | `ml-refresh-tokens.ts` cron (5h safety-net, advisory-lock-gated, exit 0 on no-row) |

### Wave 2 — Connector + API client ✓

| Plan    | Commit    | Qué hizo                                                                                          |
| ------- | --------- | ------------------------------------------------------------------------------------------------- |
| 2.1.2.1 | `d47a63c` | ML REST API client (undici + paginateOrders from_id + paginateItems scroll_id scan + 401-lazy-refresh hook + currency MCO/COP assert) |
| 2.1.2.2 | `d255151` | ML variations → product_variants mapper (additive `(master_sku, attributes_hash)` unique constraint + catalog-mode DLQ counter) |
| 2.1.2.3 | `21caa15` | `config.ts` degraded-mode discriminant: `loadMLConfig` returns `{ ok: true } | { ok: false, missing: [...] }` |
| 2.1.2.4 | `0acf236` | Connector `index.ts` REWRITE — replaces F1's NOT_IMPLEMENTED_F4 skeleton; `fetchOrders`/`fetchProducts`/`healthCheck` con degraded-mode short-circuit |

### Wave 3 — Orchestrator routes + crons ✓

| Plan    | Commit    | Qué hizo                                                                                          |
| ------- | --------- | ------------------------------------------------------------------------------------------------- |
| 2.1.3.1 | `8f09a01` | ML webhook route `/webhooks/mercadolibre` + signed-query-params HMAC verify (NOT body — divergence from WP) + raw_events dedup migration |
| 2.1.3.2 | `a1aeb74` | `sync-ml-orders.ts` cron (`*/15 * * * *`); imports `runMatchCascade` + `persistMatch` de @faka/connectors/matching — F2 W2 dependency satisfied (W2 ya había landeado al ejecutar este plan) |
| 2.1.3.3 | `94fdb39` | `sync-ml-products.ts` cron (`0 * * * *`, scroll_id pagination + variants flatten + catalog-mode skip counter) |
| 2.1.3.4 | `c6314fc` | OAuth bootstrap: dashboard `/operacion/conectar-mercadolibre` connect page + orchestrator `/oauth/mercadolibre/callback` route + `oauth_state` CSRF table (10-min TTL) |

### Wave 4 — Tests + smoke ✓

| Plan    | Commit    | Qué hizo                                                                                          |
| ------- | --------- | ------------------------------------------------------------------------------------------------- |
| 2.1.4.1 | `0af011c` | ML state-mapper exhaustive test (every documented ML status; round-trip stability) |
| 2.1.4.2 | `580d843` | ML OAuth refresh race regression test (two concurrent refresh attempts → advisory lock holds one; proof) |
| 2.1.4.3 | `e0bed87` | ML webhook signature verify exhaustive test (8 cases: tampered sig, replay-window expiry, sorted-query-params correctness, messages-topic CC-14 drop, etc.) |
| 2.1.4.4 | `8513737` | ML latency smoke + budget script (15-min E2E: t_webhook_acked → t_view_reflects); `scripts/ml-latency-smoke.ts` + `scripts/smoke-f2.1.sh` |

### Wave 5 — Docs + deploy notes ✓

| Plan    | Commit    | Qué hizo                                                                                          |
| ------- | --------- | ------------------------------------------------------------------------------------------------- |
| 2.1.5.1 | `7c7a15a` | DEPLOY.md F2.1 section: app-registration runbook + env vars table + migrations list + 3 Railway crons + OAuth bootstrap procedure + secret rotation + revoke runbook + degraded-mode matrix + troubleshooting (8 entries) |
| 2.1.5.2 | (este commit) | F2.1-PROGRESO memo (este archivo) + ROADMAP closeout (Phase 2.1 → [~] code-complete + Plans 22 + Status line) + STATE Roadmap Evolution entry |

### Diversos arreglos intermedios durante F2.1

| Commit    | Qué hizo                                                                                          |
| --------- | ------------------------------------------------------------------------------------------------- |
| `c7f64ba` | chore: gitignore `.vercel/` + `scripts/demo-bundle/` (ruido de deploy tooling)                    |

## Estado del codebase post-F2.1

- **22 plans con commit dedicado** = 22/22 plans cubiertos, F2.1 complete.
- 4 migraciones nuevas (`20260615000001_oauth_tokens.sql`,
  `20260615000001b_advisory_lock_fn.sql`,
  `20260615000002_product_variants_unique.sql`,
  `20260615000003_oauth_state.sql`).
- 1 nueva dashboard route (`/operacion/conectar-mercadolibre` + nested
  `_actions/start-oauth.ts`).
- 1 nueva orchestrator route (`/oauth/mercadolibre/callback`) + 1 nueva
  webhook route (`/webhooks/mercadolibre`).
- 3 nuevos cron services (`ml-refresh-tokens`, `sync-ml-orders`,
  `sync-ml-products`).
- F1 + F2 + F2.1 smoke (`scripts/smoke-f2.1.sh`) pasa en degraded mode (sin
  creds ML; sin orchestrator deployado).
- DEPLOY.md tiene secciones F1 + F2 + F2.1 completas.
- CC-11 + CC-12 + CC-14 invariants carried forward, todas asertadas en smoke.

## Credential reality

**ML developer app registrada 2026-05-15** en
https://developers.mercadolibre.com.co con app_id `3933497047128728` (public
OAuth client identifier — no es un secreto).

**Env vars actualmente en Vercel (orchestrator-temporarily):** `ML_CLIENT_ID`,
`ML_CLIENT_SECRET`, `ML_REDIRECT_URI`, `ML_WEBHOOK_SECRET`, `ML_SITE_ID`.
Vercel hostea ambos (dashboard + lo que será orchestrator) hasta que el
orchestrator tenga su deploy independiente a Railway.

**Posture:** F2.1 ships en degraded mode hasta que se completen DOS pasos:

1. **Production cutover:** orchestrator deploy a Railway (apps/orchestrator
   tiene Dockerfile + railway.toml listos desde F1; solo falta el push de
   creación del proyecto).
2. **OAuth bootstrap ceremony:** operator visita
   `/operacion/conectar-mercadolibre` → click "Connect Mercado Libre" →
   cliente aprueba en ML → tokens land en `oauth_tokens`. Ver DEPLOY.md
   §F2.1.4 step-by-step.

**Behavior en degraded mode (hoy):**

- `healthCheck` returns `{ ok: false, last_error: "not configured", missing: [...] }`.
- `fetchOrders` / `fetchProducts` return `[]` + structured warning; no API calls.
- `POST /webhooks/mercadolibre` → 503 `{ error: "not_configured" }`.
- `GET /oauth/mercadolibre/callback` → 503 `{ error: "not_configured" }`.
- Los tres crons (`ml-refresh-tokens`, `sync-ml-orders`, `sync-ml-products`)
  escriben `connector_runs.errors_json.reason="not_configured"` y exit 0.
- `/operacion/conectar-mercadolibre` muestra red pill "Not configured" + lista
  los env vars faltantes; sin botón de connect.
- `/hoy` + `/matching` siguen renderizando con datos no-ML (CSV + WP); sin
  slice de `mercadolibre` en per-channel chart.

Esto es by-design (ML-06) — el cliente puede tomar el tiempo que necesite
para completar la ceremonia OAuth.

## Open coordination items

1. **Move `ML_CLIENT_SECRET` + `ML_WEBHOOK_SECRET` to Railway** cuando el
   orchestrator deploye a Railway. Por ahora viven en Vercel
   orchestrator-temporarily. `ML_CLIENT_ID` + `ML_REDIRECT_URI` se quedan
   mirrored en ambos (Vercel para el dashboard server action que arma el
   authorize URL, Railway para el orchestrator que hace el code exchange).

2. **Catalog-products MCO 2026 adoption decision (Assumption A2 — LOW
   confidence).** Plan 2.1.2.2's variant-mapper DLQ'a items con
   `catalog_product_id != null` y emite un counter en `connector_runs.payload.catalog_mode_skipped`. Si el smoke
   muestra `catalog_mode_skipped > 0` per cron run → revisitar en un follow-up
   phase (decisión arquitectónica: agregar catalog-product flow, o seguir
   ignorándolo). Tracking: revisar después de 1 semana en production.

3. **F4 (Mercado Libre + Dropi + Mini-CRM) cleanup.** F4's ROADMAP entry
   todavía menciona "Mercado Libre" como primer canal del phase — eso ya
   pasó. F4 ahora es solo Dropi + Mini-CRM. Necesita un `/gsd-edit-roadmap 4`
   follow-up para sacar ML del Goal field + Success Criteria + Plans listing
   de F4. **Asignar a la próxima sesión de planning** (no bloquea F2.1
   cierre).

4. **`oauth_tokens` multi-account refactor (futuro).** Schema es multi-ready
   (`unique (canal, user_id)`) pero v1 lee/escribe 1 row only. Si en el futuro
   el cliente quiere gestionar múltiples cuentas ML (subsidiarias, partner
   accounts), el connector necesita refactor pequeño en `oauth.ts` +
   `index.ts` (parameterize user_id). No urgente.

## Decisions log (locked, from CONTEXT.md)

- **MCO + COP hardcoded.** `siteId` hardcoded `"MCO"` en types.ts; currency
  hardcoded `"COP"`. Multi-site (MLA/MLM/MLB) es deferred — no env var.
- **Cascade reuse from F2.** F2.1 NO re-implementa cascade. Importa
  `runMatchCascade` + `persistMatch` desde `@faka/connectors/matching`
  (F2 W2 output). Grep gate: zero `matchByX` imports inside
  `packages/connectors/src/mercadolibre/` (asertado por smoke).
- **`oauth_tokens` unique on (canal, user_id).** Multi-ready schema; v1
  writes 1 row.
- **Idempotency on (canal, external_order_id).** F1 invariant carried forward;
  ML orders UPSERT keyed por este compuesto.
- **No NEXT_PUBLIC_ML_* vars (CC-11 extended).** Dashboard's connect page
  arma authorize URL via server action que lee `ML_CLIENT_ID` server-side.
- **No new ML-specific dashboard pages (CC-12 carried forward).** Solo
  `/operacion/conectar-mercadolibre` (operational, no analytics). `/hoy`
  + `/matching` son channel-agnostic — ML rows surface automatically.
- **`messaging_log` stays empty (CC-14 carried forward).** ML webhook topic
  `messages` se loggea + drop con 200. ML Messaging API integration es
  F5.5 (WhatsApp Business Cloud API + ADR-003).
- **Webhook HMAC pattern divergence — keep inline.** ML's signed-query-params
  verifier es estructuralmente distinto de F2's body-bytes verifier. NO
  shared HMAC abstraction en F2.1 (PATTERNS §Open coordination items #3 +
  RESEARCH §Open Decisions #4 — POS en F3 es el third HMAC variant que
  triggerea la abstracción).

## Demo path (sin OAuth bootstrap — degraded mode)

Sin que el cliente complete la ceremonia OAuth, los demos ML-XX son
ejecutables solamente vía CSV upload usando el mismo path channel-agnostic
de F2:

1. CSV upload de una sample de ML orders (formato a definir si se necesita
   pre-bootstrap demo) por `/operacion` → `commitUpload` wizard.
2. Cascade dispara automáticamente sobre `sale_items` ingestados.
3. Items low-confidence van a `/matching` queue → manager valida → flip a
   `validado_humano=true` + audit_log row.
4. `/hoy` refleja totales/canales/top-products/live-feed (sin slice de
   `mercadolibre` hasta que OAuth bootstrap pase).

Cuando el cliente complete la ceremonia OAuth + el orchestrator deploye:

1. Restart Railway services (`orchestrator-cron-*`).
2. Wait 15min para el primer tick de `sync-ml-orders`.
3. Run smoke: `bash scripts/smoke-f2.1.sh ...` → debe pasar en CONFIGURED mode.
4. Run latency smoke: `tsx scripts/ml-latency-smoke.ts` con todas las env →
   reporte JSON con 4 timings.
5. ML-01 budget check: `t_view_reflects ≤ 15 min`.

## Known Gaps

- **No production-environment smoke validation.** Hasta que el orchestrator
  deploy a Railway + cliente complete OAuth bootstrap, el smoke solo cubre
  degraded-mode path. Configured-mode path está validado por tests unitarios
  (oauth.test.ts, webhook-verify.test.ts, state-mapper.test.ts) pero NO por
  un E2E live run.
- **Multi-site soporte (MLA/MLM/MLB) NO implementado.** F2.1 es MCO only.
  Si en el futuro el cliente expande a otras geografías, agregar `ML_SITE_ID`
  como env-driven en lugar de hardcoded const.
- **Catalog-products mode NO implementado.** Items con `catalog_product_id`
  son DLQ'd. Adoption en MCO 2026 es UNCONFIRMED (Assumption A2). Revisitar
  si counter > 0.
- **`pg` no está pinneado como dev-dep** (mismo gap que F2). `ml-latency-smoke.ts`
  hace `import("pg")` lazy y emite mensaje ejecutable si falla. Para correr
  localmente: `pnpm add -D pg @types/pg` desde repo root.

## Lo que sigue: F3 (POS + WhatsApp form + Dead Stock) ya está DESBLOQUEADO

F2.1 está code-complete. El planning de F3 está pendiente (no PLAN.md aún
para Phase 3). F3 introduce:

- POS webhooks (cliente entrega POS programmer stack + webhook willingness
  per DISC-04 — sigue pendiente).
- WhatsApp internal mini-app form (la integración Business Cloud API es
  Phase 5.5 per ADR-003; el form es permanente como fallback).
- `mart_dead_stock` promoted to MVP.
- "Operación" health-check panel real (no skeleton).
- **MVP usable milestone** — 3 channels reporting + ≥80% master_sku coverage
  + stock-muerto visible.

Cliente decisión 2026-05-14 puso F2.1 antes de F3. Una vez F2.1 entre en
production-configured mode + smoke valide ML-01..ML-06 todos en green, F3
arranca con `/gsd-plan-phase 3`.

Ver `.planning/ROADMAP.md` Phase 3 entry para el goal completo + criterios
de éxito.

## Cómo retomar (post-F2.1)

1. Lee este archivo + `memoria/F2-PROGRESO.md` (predecesor) + `.planning/ROADMAP.md`.
2. Si el cliente está listo para completar OAuth bootstrap: seguir
   `DEPLOY.md §F2.1.4` paso por paso.
3. Si no: F2.1 está congelado salvo por hot-fixes contra ML env vars
   real-creds rollout. F3 puede arrancar en paralelo (no shared code path
   bloqueante).
4. Verificar `scripts/smoke-f2.1.sh <dashboard-url> <orchestrator-url>` pasa
   antes de declarar F2.1 deployed-and-verified.

## Pause points

(Mirror del F2 pattern — executors append aquí conforme avanzan post-cierre.)

- **2026-05-15** — F2.1 code-complete 22/22 plans across 6 waves
  (W0-W5). Awaiting cliente OAuth bootstrap + orchestrator Railway deploy
  para flip a CONFIGURED mode. ML developer app registrada 2026-05-15
  (app_id 3933497047128728); env vars en Vercel (orchestrator-temporarily).
  Smoke pasa en degraded mode. **F2.1 CLOSEOUT — 100%.**
