## Conflict Detection Report

Generated by `gsd-doc-synthesizer` from `.planning/intel/classifications/`.
Mode: `new` (net-new bootstrap, no existing `.planning/` context to reconcile against).
Precedence: `["ADR", "SPEC", "PRD", "DOC"]`.

---

### BLOCKERS (0)

None. No LOCKED-vs-LOCKED contradictions detected. No cross-reference cycles detected. No UNKNOWN-confidence-low classifications.

---

### WARNINGS (0)

None. No competing acceptance variants for the same requirement scope across PRDs (only one PRD in the ingest set).

---

### INFO (1)

[INFO] Auto-resolved: ADR > PRD on CSV ingestion strategy
Found: `docs/PRD.md` §3.4 frames CSV ingestion as Dropi-only "Plan B" fallback ("Plan B: ingesta de exports CSV manuales") — applies only when the Dropi scraper fails.
Found: `docs/AMENDMENT-csv-source.md` (LOCKED, precedence 0) elevates CSV upload to a first-class, channel-agnostic, permanent data source with dedicated RAW-layer tables (`raw_csv_uploads`, `raw_csv_rows`, `csv_mapping_profiles`), a generic `CSVConnector` implementation, an Operación-view 3-step upload wizard, and explicit reshaping of Phase 0 / Phase 1 / Phase 2 / Phase 4 deliverables.
Resolution: ADR wins per precedence (0 < 2). The amendment is itself a self-declared LOCKED PRD-amendment naming `docs/PRD.md` as its base document. The PRD's "Plan B" framing is superseded; CSV is first-class everywhere in the synthesized intel.
Applied in synthesized intel: - `decisions.md` → ADR-001 records the locked decision with full scope + rationale + supersession note. - `requirements.md` → `REQ-channel-strategies` notes Dropi fallback uses `CSVConnector` (no Dropi-specific CSV code). `REQ-schema-raw-layer` adds the three CSV tables. `REQ-channel-connector-interface` registers `CSVConnector`. `REQ-dashboard-view-operacion` adds the wizard as MVP/F1. `REQ-phase-0-discovery`, `REQ-phase-1-foundation-mvp`, `REQ-phase-2-channels-and-analytics`, `REQ-phase-4-prediction-and-falabella` carry the LOCKED scope deltas. - `constraints.md` → `CONSTR-csv-connector`, `CONSTR-raw-csv-schema`, `CONSTR-csv-ingest-pipeline`, `CONSTR-csv-channel-agnostic` (all LOCKED) capture the technical contract. - `context.md` → connector-strategies cheatsheet and risk-register Dropi line updated to reflect CSV as primary backfill mechanism rather than contingency.
Rationale logged: precedence rules applied; ADR is LOCKED, PRD is not; same scope ("CSV ingestion treatment"); no conflicting LOCKED counterpart exists.
→ No user action required.

---

## Summary

- Docs synthesized: 2 (1 ADR-LOCKED, 1 PRD).
- Conflicts: 0 blockers, 0 competing variants, 1 auto-resolved (precedence-based).
- Status: READY — safe to route to `gsd-roadmapper`.
