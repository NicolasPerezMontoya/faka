{
  "name": "@faka/schema",
  "version": "0.0.0",
  "private": true,
  "description": "Shared Zod schemas + TypeScript types for the faka monorepo",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./normalize": "./src/normalize.ts"
  },
  "scripts": {
    "lint": "echo '(uses base eslint via app/package configs)'",
    "test": "vitest run",
    "test:integration": "echo '(no integration tests for pure schema)'"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@faka/config": "workspace:*",
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
