{
  "name": "@faka/llm",
  "version": "0.0.0",
  "private": true,
  "description": "Env-driven multi-provider LLM adapter (F1 LOCKED pattern). Single source of truth for arbiter / chat / completion calls across faka.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./prompts": "./src/prompts.ts"
  },
  "scripts": {
    "lint": "echo '(uses base eslint via app/package configs)'",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^2.0.0",
    "@ai-sdk/google": "^2.0.0",
    "@ai-sdk/openai": "^2.0.0",
    "@ai-sdk/openai-compatible": "^1.0.0",
    "ai": "^6.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@faka/config": "workspace:*",
    "@types/node": "^22.10.5",
    "typescript": "5.7.3",
    "vitest": "^2.1.8"
  }
}
